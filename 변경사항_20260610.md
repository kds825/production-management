# KBI Production Management - 배포 및 환경 설정 변경 기록

## 배포 환경 (2026-06-10)

| 구성 | 서비스 | URL |
|------|--------|-----|
| 프론트엔드 | Vercel | https://production-management-gules.vercel.app |
| 백엔드 | Render (Free) | https://kbi-backend.onrender.com |
| DB | Supabase (Seoul) | Session Pooler (포트 5432) |
| GitHub | kds825/production-management | 브랜치: `dev_daesung` |

## 환경변수

### 백엔드 (Render)

| Key | 설명 |
|-----|------|
| `DATABASE_URL` | Supabase Session Pooler 연결 문자열 (`postgresql+psycopg://...@...pooler.supabase.com:5432/postgres`) |
| `LLM_PROVIDER` | `openai` (PwC 게이트웨이 OpenAI 호환) |
| `OPENAI_API_KEY` | PwC GenAI SharedService 게이트웨이 키 |
| `OPENAI_API_BASE` | `https://genai-sharedservice-americas.pwcinternal.com/v1` |
| `LLM_MODEL` | `bedrock.anthropic.claude-sonnet-4-5` |
| `FEATURE_FLAG_CASCADE_V2` | `on` |

### 프론트엔드 (Vercel)

| Key | 설명 |
|-----|------|
| `NEXT_PUBLIC_API_URL` | `https://kbi-backend.onrender.com/api` |

## 코드 변경 사항

### 1. 환경 설정 파일 생성

- **`.env` (루트)** - 프론트엔드용 환경변수 (VITE_ 변수들, LLM 모델 설정)
- **`backend/.env`** - 백엔드용 환경변수 (DB, LLM, Feature Flags)
- 둘 다 `.gitignore`에 포함되어 GitHub에 올라가지 않음

### 2. DB 드라이버 전환: psycopg2 -> psycopg v3

- **원인**: 프로젝트 경로에 한글이 포함되어 psycopg2-binary가 `UnicodeDecodeError` 발생
- **변경 파일**: `backend/requirements.txt` - `psycopg[binary]>=3.1` 추가
- **변경 파일**: `backend/.env` - DATABASE_URL에 `postgresql+psycopg://` 스킴 사용
- **변경 파일**: `backend/app/infrastructure/database.py` - connect_args 정리, Session Pooler(포트 5432) 사용

### 3. Supabase 신규 프로젝트 연결

- 기존 Supabase 프로젝트(pgujccdnuidsjxuyqgyi) 만료 -> 신규 생성(qyodpuvdzzuvctbmgyrl)
- Alembic 마이그레이션 19개 적용 (`alembic upgrade head`)
- 시드 데이터 271행 삽입 (`python seed_db.py`)
- Session Pooler(포트 5432) 사용 (Transaction Pooler는 prepared statement 충돌)

### 4. LLM 프로바이더 변경: Anthropic SDK -> PwC 게이트웨이 (OpenAI 호환)

- **변경 파일**: `backend/app/infrastructure/llm/anthropic_provider.py`
  - Anthropic SDK 직접 호출 -> OpenAI SDK로 PwC 게이트웨이 호출
  - 모델: `bedrock.anthropic.claude-sonnet-4-5`
- **변경 파일**: `backend/app/infrastructure/llm/__init__.py`
  - `get_provider("openai")` 지원 추가

### 5. kiwipiepy 한글 경로 문제 해결

- **변경 파일**: `backend/app/application/decisions/narrator.py`
  - `_kiwi = Kiwi()` 모듈 레벨 초기화 -> `_get_kiwi()` lazy loading으로 변경
  - 한글 경로에서 모델 파일 로딩 실패 방지

### 6. ERP 파서 .xlsx 지원 추가

- **변경 파일**: `backend/app/infrastructure/parsers/erp_parser.py`
  - `parse_erp_file()` 함수에 openpyxl(.xlsx) 우선 시도 -> xlrd(.xls) 폴백 로직 추가
  - 기존에는 xlrd만 사용하여 .xlsx 파일 파싱 불가였음

### 7. 자동 배열 오버플로우 버그 수정

- **변경 파일**: `backend/app/application/scheduling/greedy/_assign_group.py`
  - 단일 SQ 경로에서 `datetime.max` 필터 누락 -> `pred_first < datetime.max` 조건 추가
  - 선행공정 출력이 없을 때 `earliest`가 `datetime.max`가 되어 날짜 오버플로우 발생하던 문제 해결

### 8. WIP FK 위반 수정

- **변경 파일**: `backend/app/application/ingest/pipeline_orchestrator.py`
  - WIP 파싱 전에 FK 참조(production_batch.wip_matched_id, sales_order.wip_id) NULL 화 추가
- **변경 파일**: `backend/app/presentation/routes/plan_pipeline_stage1.py`
  - 증분 업데이트 경로에서 subquery로 FK cycle 해제하도록 변경
  - `ANY(:ids)` -> `IN (SELECT ...)` 패턴으로 psycopg 호환성 확보

### 9. CORS 설정

- **변경 파일**: `backend/app/config.py`
  - `CORS_ORIGINS` 를 `["*"]`로 변경하여 모든 origin 허용 (시연용)

### 10. 프론트엔드 localhost 하드코딩 제거

- **변경 파일**: 프론트엔드 src/ 내 11개 파일
  - `const API_BASE = "http://localhost:8000/api"` -> `const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000/api"` 로 변경
  - 배포 환경에서 환경변수로 백엔드 URL 주입 가능

### 11. Render 배포 설정

- **추가 파일**: `backend/.python-version` - Python 3.13.2 고정 (3.14는 pydantic-core 빌드 실패)

## 시연 전 체크리스트

1. Render 백엔드 깨우기 (무료 플랜 15분 미사용 시 슬립)
   - `https://kbi-backend.onrender.com/api/health` 접속 -> `{"status":"ok"}` 확인
   - 시연 5분 전에 접속 권장
2. 프론트엔드 접속: `https://production-management-gules.vercel.app`
3. ERP 파일(.xlsx) 업로드 -> 작업지시서 생성 -> 자동 배열 순서로 진행

## 제한사항

- PwC 게이트웨이(`genai-sharedservice-americas.pwcinternal.com`)는 PwC 내부망에서만 접근 가능할 수 있음
  - LLM 기능(AI 설명 생성)이 외부에서 동작하지 않을 수 있으나, 스케줄링 핵심 기능은 정상 동작
- Render 무료 플랜: 15분 미사용 시 슬립, 월 750시간 제한
- CORS가 `*`(모든 origin 허용)으로 설정되어 있음 - 운영 환경에서는 특정 도메인으로 제한 필요
