"""schedules/detail.py — 단건 task / 버전 작성·수정·삭제 엔드포인트.

Week 7 Task 7A.1 단계 C 에서 create/update/delete_task + save_version 이
이 모듈로 이동된다. 현재는 빈 라우터 (sub-commit A 스캐폴드).
"""

from fastapi import APIRouter

router = APIRouter()
