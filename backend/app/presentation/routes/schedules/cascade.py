"""schedules/cascade.py — cascade preview v1/v2 엔드포인트.

Week 7 Task 7A.1 단계 E 에서 cascade_preview_legacy / cascade_preview_v2 가
이 모듈로 이동된다. 현재는 빈 라우터 (sub-commit A 스캐폴드).
"""

from fastapi import APIRouter

router = APIRouter()
