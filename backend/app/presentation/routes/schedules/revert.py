"""schedules/revert.py — revert (undo) + change_set diff 엔드포인트.

Week 7 Task 7A.1 단계 F 에서 revert / get_change_set_diff 이
이 모듈로 이동된다. 현재는 빈 라우터 (sub-commit A 스캐폴드).
"""

from fastapi import APIRouter

router = APIRouter()
