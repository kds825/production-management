"""schedules/list.py — GET 전용 목록/조회 엔드포인트.

Week 7 Task 7A.1 단계 B 에서 list_tasks / list_versions / get_version 이
이 모듈로 이동된다. 현재는 빈 라우터 (sub-commit A 스캐폴드).
"""

from fastapi import APIRouter

router = APIRouter()
