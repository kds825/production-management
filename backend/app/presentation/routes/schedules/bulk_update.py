"""schedules/bulk_update.py — bulk update v1/v2 엔드포인트.

Week 7 Task 7A.1 단계 D 에서 bulk_update_tasks_legacy / bulk_update_v2 이
이 모듈로 이동된다. 현재는 빈 라우터 (sub-commit A 스캐폴드).
"""

from fastapi import APIRouter

router = APIRouter()
