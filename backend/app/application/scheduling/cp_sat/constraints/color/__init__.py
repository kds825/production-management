"""Color constraints — sheath_color 분기.

해당 ConstraintConfig 분류: 3-2, 3-3, 3-4.
build_model 의 sheath_color_hard / sheath_color_sequence 는 process 와 색상
분기를 동시에 가지므로 process/ 카테고리에 둠 (호출 순서가 더 자연스러움).
본 디렉토리는 색상 정렬 / 색상 카탈로그 등 보조 로직 전용.
"""
