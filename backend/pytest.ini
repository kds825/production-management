[pytest]
testpaths = tests
addopts = -m "not benchmark"
markers =
    parity: slow deterministic solver regression (run with pytest -m parity)
    benchmark: env-dependent perf measurement, run with `pytest -m benchmark` (Supabase RTT-aware)
