"use client";

/**
 * TaskItem.tsx
 *
 * dnd-timeline 제거 이후 사용되지 않는 레거시 컴포넌트.
 * GanttTaskBlock.tsx로 대체되었다.
 * 다른 파일에서의 import 호환성을 위해 빈 export를 유지한다.
 */

export function TaskItem() {
  return null;
}
